<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- Definiowanie kolorów aplikacji -->
    <Color x:Key="PrimaryColor">#3498db</Color>
    <Color x:Key="SecondaryColor">#2ecc71</Color>
    <Color x:Key="AccentColor">#e74c3c</Color>

    <!-- Styl globalny dla przycisków w aplikacji -->
    <Style TargetType="Button">
        <Setter Property="BackgroundColor" Value="{StaticResource PrimaryColor}" />
        <Setter Property="TextColor" Value="White" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="HeightRequest" Value="50" />
        <Setter Property="Margin" Value="5" />
    </Style>

    <!-- Styl globalny dla etykiet w aplikacji -->
    <Style TargetType="Label">
        <Setter Property="FontSize" Value="Large" />
        <Setter Property="TextColor" Value="{StaticResource PrimaryColor}" />
    </Style>

    <!-- Styl dla ramek liczników -->
    <Style TargetType="Frame">
        <Setter Property="Padding" Value="10" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="BorderColor" Value="{StaticResource AccentColor}" />
        <Setter Property="Margin" Value="5" />
    </Style>
</ResourceDictionary>